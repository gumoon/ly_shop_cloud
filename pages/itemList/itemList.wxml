<!--itemList.wxml-->
<view class="page">
    <view class="page__bd">
      <block wx:for="{{grids}}" wx:key="title">
        <navigator url="/pages/itemDetail/itemDetail" class='il_nav'>
            <image src="{{item.pic_url}}" />
            <view>{{item.title}}</view>
        </navigator>
        </block>
    </view>
</view>
